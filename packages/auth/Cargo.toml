[package]
name = "_auth"
version = "0.0.0"
edition = "2024"

[lib]
name = "_auth"
path = "lib.rs"

[features]
postgres = []

[dependencies]
_core = { path = "../core", default-features = false }
_http = { path = "../http", default-features = false }
_rand_utils = { path = "../rand_utils", default-features = false }
async-graphql = { version = "^7.2", default-features = false }
serde = { version = "^1.0", default-features = false }
thiserror = { version = "^2.0", default-features = false }
validator = { version = "^0.20", default-features = false }
